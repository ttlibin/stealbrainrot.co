# Nginx配置示例 - 子域名多语言方案
# 将此配置添加到你的Nginx配置文件中

# 主域名 (英文)
server {
    listen 80;
    listen 443 ssl http2;
    server_name stealbrainrot.co www.stealbrainrot.co;
    
    # SSL证书配置
    ssl_certificate /path/to/ssl/certificate.crt;
    ssl_certificate_key /path/to/ssl/private.key;
    
    # 强制HTTPS重定向
    if ($scheme != "https") {
        return 301 https://$server_name$request_uri;
    }
    
    # 网站根目录
    root /var/www/stealbrainrot.co;
    index index.html;
    
    # 主要位置配置
    location / {
        try_files $uri $uri/ =404;
    }
    
    # 静态文件缓存
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}

# 中文简体子域名
server {
    listen 80;
    listen 443 ssl http2;
    server_name zh-cn.stealbrainrot.co;
    
    ssl_certificate /path/to/ssl/certificate.crt;
    ssl_certificate_key /path/to/ssl/private.key;
    
    if ($scheme != "https") {
        return 301 https://$server_name$request_uri;
    }
    
    root /var/www/stealbrainrot.co/zh-cn;
    index index.html;
    
    location / {
        try_files $uri $uri/ =404;
    }
    
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}

# 西班牙语子域名
server {
    listen 80;
    listen 443 ssl http2;
    server_name es.stealbrainrot.co;
    
    ssl_certificate /path/to/ssl/certificate.crt;
    ssl_certificate_key /path/to/ssl/private.key;
    
    if ($scheme != "https") {
        return 301 https://$server_name$request_uri;
    }
    
    root /var/www/stealbrainrot.co/es;
    index index.html;
    
    location / {
        try_files $uri $uri/ =404;
    }
    
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}

# 阿拉伯语子域名 (RTL支持)
server {
    listen 80;
    listen 443 ssl http2;
    server_name ar.stealbrainrot.co;
    
    ssl_certificate /path/to/ssl/certificate.crt;
    ssl_certificate_key /path/to/ssl/private.key;
    
    if ($scheme != "https") {
        return 301 https://$server_name$request_uri;
    }
    
    root /var/www/stealbrainrot.co/ar;
    index index.html;
    
    location / {
        try_files $uri $uri/ =404;
    }
    
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}

# 其他语言子域名配置类似...
# 韩语: ko.stealbrainrot.co
# 日语: ja.stealbrainrot.co  
# 德语: de.stealbrainrot.co
# 法语: fr.stealbrainrot.co
